# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-8#-%&nu67ov@s2*^1vsb^)g@qxb8gzw-(&ek$v492cmdg7yj-_
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings
# Uncomment and configure these settings to use PostgreSQL instead of SQLite
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=artemis
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=3600  # 1 hour in seconds
JWT_REFRESH_TOKEN_LIFETIME=86400  # 1 day in seconds

# CORS Settings
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOW_CREDENTIALS=True

# Vector Storage Settings
DEFAULT_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
DEFAULT_INDEX_TYPE=flat
VECTOR_STORAGE_PATH=media/vector_indices

# Media Settings
MEDIA_ROOT=media
